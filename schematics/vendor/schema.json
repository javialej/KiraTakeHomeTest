{
  "$schema": "http://json-schema.org/schema",
  "id": "Vendor",
  "title": "Vendor Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the vendor.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": [
    "name"
  ]
}
